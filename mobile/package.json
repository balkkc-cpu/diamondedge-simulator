{
  "name": "diamondedge-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Android wrapper for DiamondEdge (loads your live Vercel URL).",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
